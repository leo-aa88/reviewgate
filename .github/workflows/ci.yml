name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: pytest (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package, tests, and app persistence extras
        run: pip install -e ".[dev,app]"

      - name: Run tests
        run: pytest -q

  lint:
    name: Ruff lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package, tests, lint tools, and app extras
        run: pip install -e ".[dev,app]"

      - name: Run Ruff
        run: ruff check src tests

  alembic-smoke:
    name: Alembic upgrade/downgrade (Postgres 16)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: reviewgate
          POSTGRES_PASSWORD: reviewgate
          POSTGRES_DB: reviewgate
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U reviewgate -d reviewgate"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package, tests, and app persistence extras
        run: pip install -e ".[dev,app]"

      - name: Migrate, verify ORM parity, then roll back
        env:
          REVIEWGATE_DATABASE_URL: postgresql+psycopg://reviewgate:reviewgate@127.0.0.1:5432/reviewgate
        run: |
          alembic upgrade head
          pytest -q tests/test_storage_schema_parity.py
          alembic downgrade base
          pytest -q tests/test_storage_schema_post_downgrade.py

  build:
    name: build sdist + wheel
    runs-on: ubuntu-latest
    needs: [test, lint, alembic-smoke]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install build and verification tools
        run: pip install --upgrade build twine

      - name: Build distributions
        run: python -m build

      - name: Verify distributions
        run: python -m twine check dist/*

      - name: Upload distributions
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/*
          if-no-files-found: error
